# 복사 후 .env 로 저장 (저장소에 커밋하지 마세요)
PORT=3001
NODE_ENV=development

# openssl rand -base64 48 로 생성 권장 (최소 32바이트 이상)
JWT_SECRET=change-me-to-a-long-random-string

# Vite 개발 서버 출처 (프로덕션에서는 실제 도메인)
FRONTEND_ORIGIN=http://localhost:5173
